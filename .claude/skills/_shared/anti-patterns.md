# Антипаттерны

CRITICAL: .mesh, prefab без scope, StartAssetEditing без try/finally, мутация без Undo, target cast, Editor в Runtime, DestroyImmediate без Undo.
HIGH: ручной JSON, нет timeout, нет bypass, Resources.Load, Refresh в цикле, хардкод, забытый Apply/ClearProgressBar.
